@model ProCoach.Data.Club

@{
    ViewData["Title"] = "Delete club";
    Layout = "_AdminLayout";
}
    <section class="content">
        

        <h3>@ViewData["Title"]</h3>
    <div class="alter alter-danger" role="alter"><h3>are you sure you want to delete this?</h3></div>
       <div>
           <div class="row">
               <div class="col-md-6">
                   <div class="box">
                       <div class="box-header">
                           <h3 class="box-title"><i class="fa fa-info-circle"><i> @ViewData["Title"]</h3>
       </div>
                       <!-- /.box-header-->
                       <div class="box-body no-padding">
                           <table class="table table-striped">
                               <tbody>
                           <tr>
                               <td> <i class="fa fa-circle-o"></i></td>


       
          
                <td><b>@Html.DisplayFor(model => model.Address)
           
            <dt>
                @Html.DisplayNameFor(model => model.Phone)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Phone)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Website)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Website)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="ID" />
            <div class="btn-group" role="group">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            <a asp-action="Index"> class="btn-success">Back to List</a>
                <div>
        </form>
                    </div>
    
    </section>
@section  Scripts {
<partial name="_ValidationScriptPartial"/>
    }